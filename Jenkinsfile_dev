node('docker') {
  stage ('Checkout SCM') {
    cleanWs()
    checkout scm
  }



  stage ('Deploy mock-server') {
    def config = readFile 'kubernetes_dev.yml'
    sh "kubectl apply -f ${config}"
  }
}